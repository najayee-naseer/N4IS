import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getProject, projects } from "@/data/projects";
import { ProjectVisual } from "@/components/projects/project-visual";
export function generateStaticParams() { return projects.filter((project) => project.active).map(({ slug }) => ({ slug })); }
export function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { return params.then(({ slug }) => { const project = getProject(slug); return { title: project?.name ?? "Project", description: project?.description }; }); }
export default async function ProjectDetail({ params }: { params: Promise<{ slug: string }> }) { const project = getProject((await params).slug); if (!project || !project.active) notFound(); return <main id="main-content" className="page detail-page"><Link className="back-link" href="/projects">← ALL WORK</Link><header className="detail-hero"><div><p className="eyebrow">PROJECT {project.number}</p><h1>{project.name}</h1><p>{project.description}</p></div><ProjectVisual project={project} hero /></header><dl className="detail-facts"><div><dt>CATEGORY</dt><dd>{project.category}</dd></div><div><dt>STATUS</dt><dd>{project.status}</dd></div><div><dt>YEAR</dt><dd>{project.year}</dd></div><div><dt>TECHNOLOGY</dt><dd>{project.technologies.join(" / ")}</dd></div></dl><section className="narrative">{project.content.map((item) => <article key={item.label}><p className="eyebrow">{item.label}</p><h2>{item.title}</h2><p>{item.body}</p></article>)}</section><section className="detail-outro"><p className="eyebrow">PROJECT STATUS</p><h2>{project.status}</h2><p>This project is evolving. Public links will be added when there is an appropriate destination to share.</p></section></main>; }
