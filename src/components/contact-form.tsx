"use client";
import { FormEvent, useState } from "react";
export function ContactForm() { const [sent, setSent] = useState(false); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSent(true); } if (sent) return <div className="form-success" role="status"><p className="eyebrow">MESSAGE PREPARED</p><h2>THANK YOU.</h2><p>This demo form is ready for backend integration; no message has been sent from the website yet.</p><button type="button" onClick={() => setSent(false)}>SEND ANOTHER MESSAGE</button></div>; return <form className="contact-form" onSubmit={submit}><label>Name<input name="name" autoComplete="name" required /></label><label>Email<input name="email" type="email" autoComplete="email" required /></label><label>Project type<select name="projectType" defaultValue=""><option value="" disabled>Select one</option>{["Web","Mobile","AI","IoT","Software","UI/UX","Other"].map((type) => <option key={type}>{type}</option>)}</select></label><label>Message<textarea name="message" rows={5} required /></label><button type="submit">START A CONVERSATION <span>↗</span></button><p>This form is prepared for a future email or backend integration. Submitting it does not send a message today.</p></form>; }
